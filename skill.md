# Reusable Governance Dashboard Skill

The canonical reusable skill package is [`SKILL.md`](./SKILL.md). It documents the governed React/FastAPI dashboard workflow, server-derived identity and role enforcement, human approval gates, durable audit trails, healthcare IPC timelines, AI comment categories, grouped queue filters/exports, accessible tooltips, responsive layout, and export loading feedback.

Validate the package with:

```bash
python /home/ubuntu/skills/skill-creator/scripts/quick_validate.py .
```
